// Same straight-damage GroundAoe shape as dawn.ron, same circle. Sheet's
// pull-toward-center rider dropped, GroundAoe has no forced-movement field.
GroundAoe(
    energy_cost: 145.0,
    buildup_duration: 0.6,
    delay: 0.4,
    recover_duration: 0.45,
    max_range: 37.0,
    radius: 6.0,
    min_falloff: 0.4,
    damage: 62,
    poise: 15,
    knockback: (strength: 8.0, direction: Towards),
    dodgeable: Roll,
    reagent: None,
    rooted_cast: false,
    meta: (
        school: Some(Evocation),
        source: Some(Arcane),
        // level 5 spell -> min_level 30, the level * 6 convention.
        requirements: (min_level: Some(30)),
    ),
)
