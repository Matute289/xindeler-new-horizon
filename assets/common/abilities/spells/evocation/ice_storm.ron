// Reviewer correction: originally authored as Explosion, which always
// detonates centered on the caster (no target-point field exists) --
// contradicted this spell own longer cast range. Converted to GroundAoe,
// the real detonate-at-a-chosen-point primitive.
// Straight-damage Explosion, scaled to circle 4 on the
// fireball.ron->hemoclysm.ron curve. Sheet's save-for-half rider dropped,
// no partial-damage-on-save primitive exists.
GroundAoe(
    energy_cost: 105.0,
    buildup_duration: 0.7,
    delay: 0.4,
    max_range: 91.0,
    recover_duration: 0.5,
    damage: 55,
    poise: 15,
    knockback: (direction: Away, strength: 5.0),
    radius: 6,
    min_falloff: 0.35,
    dodgeable: Roll,
    rooted_cast: false,
    reagent: Some(Blue),
    meta: (
        school: Some(Evocation),
        source: Some(Arcane),
        // level 4 spell -> min_level 24, the level * 6 convention.
        requirements: (min_level: Some(24)),
    ),
)
