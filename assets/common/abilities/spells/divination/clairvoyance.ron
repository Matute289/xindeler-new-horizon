SelfBuff(
    buildup_duration: 0.5,
    cast_duration: 0.5,
    recover_duration: 0.3,
    buffs: [
        BuffDesc(
            kind: RemoteSensing,
            data: BuffData(
                strength: 0.0,
                duration: Some(60.0),
                misc_data: Some(RemoteSense(anchor_kind: Sensor, free_look: true, piloted: false)),
            ),
        ),
    ],
    energy_cost: 20,
    enforced_limit: false,
    meta: (
        school: Some(Divination),
        source: Some(Arcane),
        // level 3 spell -> min_level 18, same `level * 6` convention
        // `see_invisibility`/`true_seeing` already use.
        requirements: (min_level: Some(18)),
    ),
)
